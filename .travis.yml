language: bash
services: docker

after_success:
  # Log that the build worked, because we all need some good news
  - echo "Success running multi-ln-demo"

install:
  - docker build --tag=multilndemo .

script:
  - docker run multilndemo
