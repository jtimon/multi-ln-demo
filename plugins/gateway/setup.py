from distutils.core import setup

setup(
    name = "gateway-plugin",
    version = "0.0.1",
    description='A gateway plugin to facilitate lightning payments across several different chains',
    author='Jorge Timón',
    packages=['gateway_plugin'],
)
