from distutils.core import setup

setup(
    name = "py-ln-gateway",
    version = "0.0.1",
    description='A gateway server to facillitate lightning payments across several different chains',
    author='Jorge Timón',
    packages=['py_ln_gateway'],
)
